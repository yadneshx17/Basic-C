#include <stdio.h>

int main () {
    printf("The Size of the Integer is: %zu\n", sizeof(int));
    printf("The Size of the Float is: %zu\n", sizeof(float));
    printf("The Size of the Double is: %zu\n", sizeof(double));
    printf("The Size of the Char is: %zu", sizeof(char));
}